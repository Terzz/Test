services:
  fripe:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    env_file: ../.env
    environment:
      DATA_DIR: /app/data
    volumes:
      - ../data:/app/data
    restart: unless-stopped
